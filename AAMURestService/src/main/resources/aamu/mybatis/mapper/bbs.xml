<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="aamu.mybatis.mapper.bbs">
	<!-- 글 목록용 -->
	<select id="bbsSelectList" resultType="bbsDTO">
		SELECT rbbs.*, rbp.photo, rb.*, r.* FROM routebbs rbbs JOIN routebbsphoto rbp ON rbbs.rbn = rbp.rbn JOIN routeboard rb ON rbbs.rbn = rb.rbn JOIN route r ON rb.rbn = r.rbn
	</select>
	<!-- 글 목록용_사진 뿌려주기 -->
	<select id="bbsSelectPhotoList" parameterType="int" resultType="String">
		SELECT photo FROM routebbsphoto WHERE rbn=#{rbn}
	</select>
	
	
	
	
	
</mapper>