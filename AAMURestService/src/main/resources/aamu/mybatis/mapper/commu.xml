<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="aamu.mybatis.mapper.adminCommu">

	<!-- 관리자 커뮤니티 게시글 -->
	<select id="commuSelectList" resultType="commuDTO">
		SELECT *.c , u.userprofile, cc.cno, cc.id, cc.reply, photo
		FROM community c JOIN users u ON c.id = u.id
		JOIN commucomment cc ON c.lno = cc.lno
		JOIN commuphoto cp ON c.lno = cp.lno
		ORDER BY lno DESC
	</select>
	
	
</mapper>