<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.mybatis.mapper.users">
	<!-- 전체 글 뿌려주기 -->
	<!--  
	<select id="commuSelectList"  parameterType="Map" resultType="usersDTO">
		SELECT * FROM
		(SELECT T.*,ROWNUM R FROM (
		SELECT c.* FROM community c JOIN users u ON c.id=u.id 
		<if test="searchColumn != null">
	  	 	WHERE ${searchColumn} LIKE '%' || #{searchWord} || '%' 
	  	</if>  
		ORDER BY lno DESC) T)
		WHERE R BETWEEN #{start} AND #{end}
	</select>
	-->
	
	<!-- 전체 글 레코드수 -->
	<!--  
	<select id="commuGetTotalRecordCount" parameterType="Map" resultType="int">
	 	SELECT COUNT(*) FROM community
	</select>
	-->
	
	
	
	
</mapper>